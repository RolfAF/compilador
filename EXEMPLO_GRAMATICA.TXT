int func[int min , int max][
    
    int num = 0
    for[int i = min; i -le max; i++] [
        
        num = [i * 5]
        if[num -ge 25][
            num++
        ]else[
            num--
        ]
        out[num]
        
    ]
    
]

int main[][
    func[1,10]
]
